import { type ReactElement } from "react";

const RecordDetailsSection = (): ReactElement => {
    // const { state, dispatch, data } = useCatalogRecords();

    return <div>Record Details Placeholder</div>;
};

export default RecordDetailsSection;
