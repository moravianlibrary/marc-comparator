[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    db: test requires only Postgres
    es: test requires Postgres + Elasticsearch
    redis: test requires Postgres + Elasticsearch + Redis
